# Example environment file for the project.
# Copy this to `.env` and fill real values (do NOT commit `.env`).

# WebSocket服务端配置
NAPCAT_WS_URL=ws://localhost:8080/qq

# HTTP事件接收服务配置
FLASK_HOST=0.0.0.0
FLASK_PORT=20010

# API Token配置（可选）
# 如果NapCat配置了Token验证，请在此处设置相同的Token
# 留空表示不启用Token验证
API_TOKEN=YOUR_API_TOKEN_HERE

# 漫画下载路径（相对仓库根目录）
MANGA_DOWNLOAD_PATH=./downloads
